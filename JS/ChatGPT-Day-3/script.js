// console.log('JS Initializing!!!!');

// Day 3: Arrays, Loops, map(), filter(), reduce()

// Arrays
// const fruits = ['apple', 'banana', 'mango']

// Loops

// fruits.forEach(e => {
//     console.log(e);
// });

// map()
// Naya array return karta hai
// Har value ko modify karke new version deta hai

// const num = [200, 150, 100]

// const discount = num.map(p => p - 50);

// console.log(discount);

// filter()

// console.log('filter() Sirf wo items return karta hai jo condition pass karte hain');

// const filt = [50, 70, 40, 20, 200, 150, 100];

// const filt_map = filt.filter(p => (p >= 50));

// console.log(filt_map);

// reduce()

// console.log('reduce() Sab values ko combine karta hai → e.g. sum, total, average, etc.');

// const list = [120, 200, 450, 670]

// const list_red = list.reduce((a, b) => a + b)

// console.log(list_red);
